<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if (posts.length) { %>
    <% posts.forEach( post => {  %>
    <div><h3><%= post.title %></h3></div> <hr>
    <div><p><%= post.description %></p></div> <hr>
    <div><small><%= post.author %></small></div> <hr>
    <% }) %>
    <% } else {  %>
        <h1>No post found</h1>
        <% } %>
</body>
</html>